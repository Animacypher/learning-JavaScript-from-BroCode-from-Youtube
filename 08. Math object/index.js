//Math = built-in object that provides a collection of properties and methods

/*
console.log(Math.PI); //3.141592653589793
 console.log(Math.E); //2.718281828459045
 */

 let x = 3.99;
 let y = 2;
 let z;

//z = Math.round(x); //rounds to the nearest integer
//z = Math.floor(x); //rounds down to the nearest integer
//z = Math.ceil(x); //rounds up to the nearest integer
//z = Math.pow(x, y); //x to the power of y
//z = Math.sqrt(x); //square root
//z = Math.log(x); //natural logarithm of x
//z = Math.sin(x); //sine of x (x in radians)
//z = Math.cos(x); //cosine of x (x in radians)
//z = Math.tan(x); //tangent of x (x in radians)
//z = Math.abs(x); //absolute value of x
//z = Math.sign(x); //sign of x, returns 1 if positive, -1 if negative, 0 if zero
let max = Math.max(x, y);
let min = Math.min(x, y);
console.log(z);
console.log(max);
console.log(min);